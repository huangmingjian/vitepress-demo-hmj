import { _ as _export_sfc, c as createElementBlock, o as openBlock, ae as createStaticVNode } from "./chunks/framework.D7BYLF8s.js";
const __pageData = JSON.parse('{"title":"1. 删除旧的错误记录","description":"","frontmatter":{},"headers":[],"relativePath":"mysql.md","filePath":"mysql.md"}');
const _sfc_main = { name: "mysql.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, _cache[0] || (_cache[0] = [
    createStaticVNode('<h3 id="docker-mysql" tabindex="-1">docker mysql <a class="header-anchor" href="#docker-mysql" aria-label="Permalink to &quot;docker mysql&quot;">​</a></h3><h1 id="_1-删除旧的错误记录" tabindex="-1">1. 删除旧的错误记录 <a class="header-anchor" href="#_1-删除旧的错误记录" aria-label="Permalink to &quot;1. 删除旧的错误记录&quot;">​</a></h1><p>ssh-keygen -R 121.40.22.192</p><h1 id="_2-重新连接并接受新指纹" tabindex="-1">2. 重新连接并接受新指纹 <a class="header-anchor" href="#_2-重新连接并接受新指纹" aria-label="Permalink to &quot;2. 重新连接并接受新指纹&quot;">​</a></h1><p>ssh <a href="mailto:root@121.40.22.192" target="_blank" rel="noreferrer">root@121.40.22.192</a></p><h1 id="输入-yes-接受新指纹" tabindex="-1">输入 yes 接受新指纹 <a class="header-anchor" href="#输入-yes-接受新指纹" aria-label="Permalink to &quot;输入 yes 接受新指纹&quot;">​</a></h1><h3 id="mysql数据库" tabindex="-1">mysql数据库 <a class="header-anchor" href="#mysql数据库" aria-label="Permalink to &quot;mysql数据库&quot;">​</a></h3><p>// 进入mysql mysql -uroot -p123456 // 修改数据库密码 updata mysql.user set authentication_string=password(&#39;123456&#39;) where user=&#39;root&#39; and host=&#39;localhost&#39;; flush privileges; // 刷新权限 show databases; // 显示所有数据库 create database test; // 创建数据库 use test; // 使用数据库 show tables; // 显示所有表 describe test; // 显示表结构 exit; // 退出 -- 单行注释 /* 多行注释 */ create database if not exists test; // 创建数据库，如果数据库已存在则不创建 drop database if exists test; // 删除数据库，如果数据库不存在则不删除 create table if not exists test; // 创建表 drop table if exists test; // 删除表</p><h3 id="数值" tabindex="-1">数值 <a class="header-anchor" href="#数值" aria-label="Permalink to &quot;数值&quot;">​</a></h3><p>tinyint // 1字节 范围 -128 ~ 127 smallint // 2字节 范围 -32768 ~ 32767 mediumint // 3字节 范围 -8388608 ~ 8388607 int // 4字节 范围 -2147483648 ~ 2147483647 bigint // 8字节 范围 -9223372036854775808 ~ 9223372036854775807 float // 单精度浮点数 4字节 double // 双精度浮点数 8字节 decimal // 高精度定点数 可指定精度和位数</p><h3 id="字符串" tabindex="-1">字符串 <a class="header-anchor" href="#字符串" aria-label="Permalink to &quot;字符串&quot;">​</a></h3><p>char // 固定长度的字符串 0 ~ 255 varchar // 可变长字符串 0 ~ 65535 varchar(255) // 可变长字符串 tinytext // 短文本字符串 0 ~ 255 text // 长文本字符串 0 ~ 65535</p><h3 id="日期和时间" tabindex="-1">日期和时间 <a class="header-anchor" href="#日期和时间" aria-label="Permalink to &quot;日期和时间&quot;">​</a></h3><p>date // 日期 0000-00-00 ~ 9999-12-31 time // 时间 00:00:00 ~ 23:59:59 datetime // 日期和时间 0000-00-00 00:00:00 ~ 9999-12-31 23:59:59 timestamp // 时间戳 1970-01-01 00:00:00 ~ 2038-01-19 03:14:07 year // 年份 1901 ~ 2155</p><h3 id="null" tabindex="-1">null <a class="header-anchor" href="#null" aria-label="Permalink to &quot;null&quot;">​</a></h3><p>null // 空值</p><h3 id="字段属性" tabindex="-1">字段属性 <a class="header-anchor" href="#字段属性" aria-label="Permalink to &quot;字段属性&quot;">​</a></h3><p>UNSIGNED // 无符号,不能输入负数 unsigned ZEROFILL // 零填充,数字前面补0 zerofill AUTO_INCREMENT // 自增 auto_increment NOT NULL // 不能为空 not null DEFAULT &#39;值&#39; // 默认值 default &#39;值&#39; COMMENT &#39;注释&#39; // 注释 comment &#39;注释&#39; PRIMARY KEY // 主键 primary key UNIQUE KEY // 唯一索引 unique key KEY // 索引 key FOREIGN KEY // 外键 foreign key</p><h3 id="创建表" tabindex="-1">创建表 <a class="header-anchor" href="#创建表" aria-label="Permalink to &quot;创建表&quot;">​</a></h3><h4 id="语法" tabindex="-1">语法 <a class="header-anchor" href="#语法" aria-label="Permalink to &quot;语法&quot;">​</a></h4><p>create table if not exists <code>表名</code> ( <code>字段名</code> 数据类型 [属性] [索引] [注释], <code>字段名</code> 数据类型 [属性] [索引] [注释] ) [引擎] [字符集] [注释];</p><h4 id="示例" tabindex="-1">示例 <a class="header-anchor" href="#示例" aria-label="Permalink to &quot;示例&quot;">​</a></h4><p>create table if not exists <code>test</code> ( -- 表名 <code>id</code> int unsigned not null auto_increment comment &#39;id&#39;, -- 字段名 数据类型:整数类型 unsigned:无符号 not null:不能为空 default:默认值 comment:注释 <code>age</code> tinyint unsigned not null default 0 comment &#39;年龄&#39;, -- 字段名 数据类型:字符串类型 not null:不能为空 default:默认值 comment:注释 <code>name</code> varchar(255) not null default &#39;&#39; comment &#39;名称&#39;, -- 字段名 数据类型:日期和时间类型 not null:不能为空 default:默认值 comment:注释 <code>create_time</code> datetime not null default CURRENT_TIMESTAMP comment &#39;创建时间&#39;, -- 字段名 数据类型:日期和时间类型 not null:不能为空 default:默认值 comment:注释 <code>update_time</code> timestamp not null default CURRENT_TIMESTAMP on update CURRENT_TIMESTAMP comment &#39;更新时间&#39;, -- 字段名 数据类型:日期和时间类型 not null:不能为空 default:默认值 comment:注释 primary key (<code>id</code>) -- 主键 )engine=innodb default charset=utf8mb4 comment=&#39;测试表&#39;; -- 表结构 engine:存储引擎 default:默认值 charset:字符集 comment:注释</p><h4 id="语句获取" tabindex="-1">语句获取 <a class="header-anchor" href="#语句获取" aria-label="Permalink to &quot;语句获取&quot;">​</a></h4><p>show create table <code>表名</code>; // 获取创建表的语句 show create database <code>数据库名</code>; // 获取数据库创建的语句 desc <code>表名</code>; // 获取表结构</p><h4 id="引擎" tabindex="-1">引擎 <a class="header-anchor" href="#引擎" aria-label="Permalink to &quot;引擎&quot;">​</a></h4><p>engine=innodb; // innodb引擎 支持事务,外键,行级锁,安全性高(默认) engine=myisam; // myisam引擎 不支持事务,外键,行级锁,安全性低 engine=memory; // memory引擎 内存引擎,数据存储在内存中,性能高,数据丢失 engine=aria; // aria引擎 兼容myisam引擎,支持事务,外键,行级锁,安全性高 engine=tokudb; // tokudb引擎 兼容innodb引擎,支持事务,外键,行级锁,安全性高</p><p>engine=ndbcluster; // ndbcluster引擎 兼容innodb引擎,支持事务,外键,行级锁,安全性高 主要用于分布式数据库</p><ul><li>NDB Cluster 引擎 专为集群环境设计,支持分布式数据库,提供高可用性和分区容忍性,适用于需要水平扩展的应用场景</li></ul><p>engine=oceanbase; // oceanbase引擎 兼容innodb引擎,支持事务,外键,行级锁,安全性高 主要用于分布式数据库</p><ul><li>OceanBase 引擎,阿里巴巴开发的分布式关系型数据库,兼容 MySQL 协议，支持分布式事务,主要用于大规模分布式数据库场景 percona - Percona 引擎</li></ul><p>engine=percona; // percona引擎 兼容innodb引擎,支持事务,外键,行级锁,安全性高 主要用于分布式数据库 -Percona 公司优化的 MySQL 版本,性能增强和高级监控功能,实际上不是一个独立的存储引擎，而是 MySQL 的发行版,搜索和空间索引引擎</p><p>engine=mroonga; // mroonga引擎 兼容innodb引擎,支持事务,外键,行级锁,安全性高 全文搜索</p><ul><li>Groonga 集成引擎,针对全文搜索优化,基于 Groonga 搜索库,提供比 MySQL 内置全文索引更好的搜索性能,geometry 和 spatial - 空间数据引擎</li></ul><p>engine=geometry; // geometry引擎 兼容innodb引擎,支持事务,外键,行级锁,安全性高 空间索引</p><ul><li>专门处理地理空间数据,支持空间索引，提高地理位置查询效率,用于 GIS（地理信息系统）应用</li></ul><p>engine=spatial; // spatial引擎 兼容innodb引擎,支持事务,外键,行级锁,安全性高 空间索引 engine=federated; // federated引擎 兼容innodb引擎,支持事务,外键,行级锁,安全性高 跨数据库查询 engine=archive; // archive引擎 兼容innodb引擎,支持事务,外键,行级锁,安全性高 压缩存储</p><h3 id="修改表" tabindex="-1">修改表 <a class="header-anchor" href="#修改表" aria-label="Permalink to &quot;修改表&quot;">​</a></h3><p>alter table <code>表名</code> rename to <code>新表名</code>; // 修改表名 alter table <code>表名</code> add column <code>字段名</code> 数据类型 [属性] [索引] [注释]; // 添加字段 alter table <code>表名</code> modify column <code>字段名</code> 数据类型 [属性] [索引] [注释]; // 修改字段约束 alter table <code>表名</code> change column <code>字段名</code> <code>新字段名</code> 数据类型 [属性] [索引] [注释]; // 修改字段名 alter table <code>表名</code> drop column <code>字段名</code>; // 删除字段 alter table <code>表名</code> add constraint <code>约束名</code> foreign key (<code>字段名</code>) references <code>关联表名</code>(<code>关联字段名</code>); // 添加外键 foreign key: 自己的字段名 references: 关联的表名关联的字段名 constraint: 约束名</p><h3 id="插入数据" tabindex="-1">插入数据 <a class="header-anchor" href="#插入数据" aria-label="Permalink to &quot;插入数据&quot;">​</a></h3><p>insert into <code>表名</code> (<code>字段名1</code>, <code>字段名2</code>) values (<code>值1</code>, <code>值2</code>); // 插入数据 insert into <code>表名</code> values (<code>值1</code>, <code>值2</code>),(<code>值1</code>, <code>值2</code>),(<code>值1</code>, <code>值2</code>); // 批量插入数据</p><h3 id="修改数据" tabindex="-1">修改数据 <a class="header-anchor" href="#修改数据" aria-label="Permalink to &quot;修改数据&quot;">​</a></h3><p>update <code>表名</code> set <code>字段名1</code> = <code>值1</code>, <code>字段名2</code> = <code>值2</code> where <code>条件</code>; // 修改数据</p><h3 id="where-条件" tabindex="-1">where 条件 <a class="header-anchor" href="#where-条件" aria-label="Permalink to &quot;where 条件&quot;">​</a></h3><p>where <code>字段名</code> = <code>值</code>; // 等于 where <code>字段名</code> &lt;&gt; <code>值</code>; // 不等于 where <code>字段名</code> &gt; <code>值</code>; // 大于 where <code>字段名</code> &gt;= <code>值</code>; // 大于等于 where <code>字段名</code> &lt; <code>值</code>; // 小于 where <code>字段名</code> &lt;= <code>值</code>; // 小于等于 where <code>字段名</code> like &#39;%值%&#39;; // 模糊匹配 % 表示任意字符 _ 匹配一个字符 where <code>字段名</code> in (<code>值1</code>, <code>值2</code>, <code>值3</code>); // 批量匹配 where <code>字段名</code> between <code>值1</code> and <code>值2</code>; // 范围匹配 where <code>字段名</code> is null; // 为 null where <code>字段名</code> is not null; // 不为 null order by <code>字段名</code> asc; // 升序 limit 0,10; //第1页数据，10条数据 group by <code>字段名</code>; // 分组 where <code>字段名</code> regexp &#39;正则表达式&#39;; // 正则匹配 where <code>字段名</code> between &#39;2021-01-01&#39; and &#39;2021-12-31&#39;; // 时间范围匹配 where <code>字段名</code> between &#39;2021-01-01&#39; and &#39;2021-12-31&#39; and <code>字段名2</code> = &#39;值2&#39;; // 多条件匹配 where <code>字段名</code> between &#39;2021-01-01&#39; and &#39;2021-12-31&#39; or <code>字段名2</code> = &#39;值2&#39;; // 多条件匹配 where <code>字段名</code> between &#39;2021-01-01&#39; and &#39;2021-12-31&#39; and <code>字段名2</code> = &#39;值2&#39; or <code>字段名3</code> = &#39;值3&#39;; // 多条件匹配 where <code>字段名</code> between &#39;2021-01-01&#39; and &#39;2021-12-31&#39; and <code>字段名2</code> = &#39;值2&#39; or <code>字段名3</code> = &#39;值3&#39; limit 10; // 多条件匹配 limit: 限制条数 where <code>字段名</code> between &#39;2021-01-01&#39; and &#39;2021-12-31&#39; and <code>字段名2</code> = &#39;值2&#39; or <code>字段名3</code> = &#39;值3&#39; limit 10 offset 10; // 多条件匹配 limit: 限制条数 offset: 跳过条数 where <code>字段名</code> between &#39;2021-01-01&#39; and &#39;2021-12-31&#39; and <code>字段名2</code> = &#39;值2&#39; or <code>字段名3</code> = &#39;值3&#39; limit 10 offset 10 order by <code>字段名</code> asc; // 多条件匹配 limit: 限制条数 offset: 跳过条数 order by: 排序 asc: 升序 desc: 降序</p><h3 id="删除数据" tabindex="-1">删除数据 <a class="header-anchor" href="#删除数据" aria-label="Permalink to &quot;删除数据&quot;">​</a></h3><p>delete from <code>表名</code> where <code>条件</code>; // 删除数据 delete from <code>表名</code>; // 删除所有数据 truncate table <code>表名</code>; // 删除所有数据 不影响事务 自增重置</p><h3 id="查询数据-dql" tabindex="-1">查询数据 DQL <a class="header-anchor" href="#查询数据-dql" aria-label="Permalink to &quot;查询数据 DQL&quot;">​</a></h3><p>select * from <code>表名</code>; // 查询所有数据 select <code>字段名1</code>, <code>字段名2</code> from <code>表名</code>; // 查询指定字段 select <code>字段名1</code> as <code>别名</code>, <code>字段名2</code> as <code>别名2</code> from <code>表名</code>; select <code>字段名1</code>, <code>字段名2</code> from <code>表名</code> where <code>条件</code>; // 查询指定字段 条件 select <code>字段名1</code>, <code>字段名2</code> from <code>表名</code> where <code>条件</code> group by <code>字段名1</code>; // 查询指定字段 批量条件 group by: 分组 select <code>字段名1</code>, <code>字段名2</code> from <code>表名</code> where <code>条件</code> group by <code>字段名1</code> having <code>条件</code>; // 批量条件 having: 分组条件 select <code>字段名1</code>, <code>字段名2</code> from <code>表名</code> where <code>条件</code> order by <code>字段名1</code> asc; // 批量条件 排序 asc: 升序 desc: 降序 select <code>字段名1</code>, <code>字段名2</code> from <code>表名</code> where <code>条件</code> limit 10; // 批量条件 limit: 限制条数 select <code>字段名1</code>, <code>字段名2</code> from <code>表名</code> where <code>条件</code> limit 10 offset 10; // 批量条件 limit: 限制条数 offset: 跳过条数 select <code>字段名1</code>, <code>字段名2</code> from <code>表名</code> where <code>条件</code> limit 10 offset 10 order by <code>字段名1</code> asc; // 批量条件 limit: 限制条数 offset: 跳过条数 order by: 排序 asc: 升序 desc: 降序</p><h3 id="联表查询" tabindex="-1">联表查询 <a class="header-anchor" href="#联表查询" aria-label="Permalink to &quot;联表查询&quot;">​</a></h3><p>select * from <code>表名1</code> join <code>表名2</code> on <code>表名1</code>.<code>字段名1</code> = <code>表名2</code>.<code>字段名2</code>; // 联表查询 join: 内连接 left join: 左连接 right join: 右连接 full join: 全连接 on: 连接条件 // inner join: 内连接</p><h4 id="例如-inner-join" tabindex="-1">例如 inner join <a class="header-anchor" href="#例如-inner-join" aria-label="Permalink to &quot;例如 inner join&quot;">​</a></h4><p>select <code>别名1</code>.<code>字段名</code>,<code>字段名2</code>,<code>字段名3</code> from <code>表名1</code> as <code>别名1</code> inner join <code>表名2</code> as <code>别名2</code> on <code>表名1</code>.<code>字段名1</code> = <code>表名2</code>.<code>字段名2</code>; // 查寻2个表的的字段，只有两个表有相同字段时才会返回,两个表都必须有相同字段的匹配值才会返回,如果某条记录在任一表中找不到对应匹配，则不会出现在结果中,以少的表为基准</p><h4 id="例如-left-join" tabindex="-1">例如 left join <a class="header-anchor" href="#例如-left-join" aria-label="Permalink to &quot;例如 left join&quot;">​</a></h4><p>select <code>别名1</code>.<code>字段名</code>,<code>字段名2</code>,<code>字段名3</code> from <code>表名1</code> as <code>别名1</code> left join <code>表名2</code> as <code>别名2</code> on <code>表名1</code>.<code>字段名1</code> = <code>表名2</code>.<code>字段名2</code>; // 查询2个表，返回第一个表所有字段，第二个表有相同字段时返回，以表名1为基准,表名2不存在的字段返回null</p><h4 id="例如-right-join" tabindex="-1">例如 right join <a class="header-anchor" href="#例如-right-join" aria-label="Permalink to &quot;例如 right join&quot;">​</a></h4><p>select <code>别名1</code>.<code>字段名</code>,<code>字段名2</code>,<code>字段名3</code> from <code>表名1</code> as <code>别名1</code> right join <code>表名2</code> as <code>别名2</code> on <code>表名1</code>.<code>字段名1</code> = <code>表名2</code>.<code>字段名2</code>; // 查询2个表，返回第二个表所有字段，第一个表有相同字段时返回,以表名2为基准,表名1不存在的字段返回null</p><h4 id="例如-full-join" tabindex="-1">例如 full join <a class="header-anchor" href="#例如-full-join" aria-label="Permalink to &quot;例如 full join&quot;">​</a></h4><p>select <code>别名1</code>.<code>字段名</code>,<code>字段名2</code>,<code>字段名3</code> from <code>表名1</code> as <code>别名1</code> full join <code>表名2</code> as <code>别名2</code> on <code>表名1</code>.<code>字段名1</code> = <code>表名2</code>.<code>字段名2</code>; // 批量查询，返回2个表所有字段，有相同字段时返回,返回两个表的所有字段，不管是否匹配,对于没有匹配的字段会显示为 NULL,以多个表时，返回所有字段，不管是否匹配,对于没有匹配的字段会显示为 NULL,以多个表为基准 //MySQL 不支持原生 FULL JOIN 使用下面 UNION SELECT s.name, s.age, c.course_name, c.score FROM students s LEFT JOIN courses c ON s.id = c.student_id UNION SELECT s.name, s.age, c.course_name, c.score FROM students s RIGHT JOIN courses c ON s.id = c.student_id;</p><h3 id="联表查询实例" tabindex="-1">联表查询实例 <a class="header-anchor" href="#联表查询实例" aria-label="Permalink to &quot;联表查询实例&quot;">​</a></h3><p>CREATE TABLE students2 ( id INT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(50) NOT NULL, age TINYINT UNSIGNED DEFAULT 0 COMMENT &#39;年龄&#39;, class VARCHAR(20) DEFAULT &#39;&#39; COMMENT &#39;班级&#39; ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT=&#39;学生表&#39;;</p><p>INSERT INTO students2 (name, age, class) VALUES (&#39;张三&#39;, 20, &#39;计算机1班&#39;), (&#39;李四&#39;, 21, &#39;计算机2班&#39;), (&#39;王五&#39;, 19, &#39;数学系&#39;), (&#39;赵六&#39;, 22, &#39;物理系&#39;), (&#39;钱七&#39;, 20, &#39;化学系&#39;);</p><p>INSERT INTO students2 (name, age, class) VALUES (&#39;张三&#39;, 20, &#39;计算机1班&#39;);</p><p>CREATE TABLE courses ( course_id INT PRIMARY KEY AUTO_INCREMENT, course_name VARCHAR(100) NOT NULL, student_id INT, score DECIMAL(5,2) ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT=&#39;课程成绩表&#39;;</p><p>-- 插入课程数据 INSERT INTO courses (course_name, student_id, score) VALUES (&#39;高等数学&#39;, 1, 85.5), (&#39;线性代数&#39;, 1, 92.0), (&#39;大学物理&#39;, 2, 78.5), (&#39;计算机基础&#39;, 3, 88.0), (&#39;数据结构&#39;, 1, 95.0);</p><p>INSERT INTO courses (course_name, student_id, score) VALUES (&#39;高等数学sss&#39;, 88, 85.5);</p><p>select id,name,age,score,course_name from students2 as s inner join courses c on s.id = c.student_id</p><p>select id,name,age,score,course_name from students2 as s left join courses c on s.id = c.student_id UNION select id,name,age,score,course_name from students2 as s right join courses c on s.id = c.student_id</p><h3 id="聚合函数" tabindex="-1">聚合函数 <a class="header-anchor" href="#聚合函数" aria-label="Permalink to &quot;聚合函数&quot;">​</a></h3><p>select count(*); // 总记录数,查全部行,不忽略null select count(<code>字段名</code>); // 字段记录数,查全部行,忽略null select count(1); // 总记录数,查全部行,不忽略null select count(distinct <code>字段名</code>); // 字段记录数,查全部行,忽略null,去重 select sum(<code>字段名</code>) from <code>表名</code>; // 查询字段求和 select avg(<code>字段名</code>) from <code>表名</code>; // 批量条件 查询字段平均值 select max(<code>字段名</code>) from <code>表名</code>; // 批量条件 查询字段最大值 select min(<code>字段名</code>) from <code>表名</code>; // 批量条件 查询字段最小值 select distinct <code>字段名</code> from <code>表名</code>; // 批量条件 查询字段去重 select concat(<code>字段名1</code>, <code>字段名2</code>) as <code>别名</code> from <code>表名</code>; // 拼接字段为一列 select VERSION(); // 查询数据库版本 select 100 + 200 as <code>结果</code>; // 计算 select @@auto_increment_increment; // 查询自增步长 select <code>字段名</code>+1 from <code>表名</code> where <code>字段名</code> = &#39;值&#39; // 字段加1</p><h3 id="分组和过滤" tabindex="-1">分组和过滤 <a class="header-anchor" href="#分组和过滤" aria-label="Permalink to &quot;分组和过滤&quot;">​</a></h3><p>select <code>字段名1</code>, <code>字段名2</code> from <code>表名</code> where <code>条件</code> group by <code>字段名1</code>; // 批量条件 select <code>字段名1</code>, <code>字段名2</code> from <code>表名</code> where <code>条件</code> group by <code>字段名1</code> having <code>条件</code>; // 批量条件 having: 过滤,分组后的条件过滤，where:分组前的条件过滤</p><h3 id="加密" tabindex="-1">加密 <a class="header-anchor" href="#加密" aria-label="Permalink to &quot;加密&quot;">​</a></h3><p>select md5(&#39;密码&#39;); update <code>表名</code> set <code>字段名</code> = md5(&#39;密码&#39;) where <code>条件</code>; select * from <code>表名</code> where <code>字段名</code> = md5(&#39;密码&#39;);</p><h3 id="事务" tabindex="-1">事务 <a class="header-anchor" href="#事务" aria-label="Permalink to &quot;事务&quot;">​</a></h3><p>set autocommit = 0; // 关闭自动提交 1：自动提交 0：手动提交 start transaction; // 开启事务 commit; // 提交事务 rollback; // 回滚事务 set autocommit = 1; // 开启自动提交</p><h3 id="索引" tabindex="-1">索引 <a class="header-anchor" href="#索引" aria-label="Permalink to &quot;索引&quot;">​</a></h3><p>create index <code>索引名</code> on <code>表名</code>(<code>字段名</code>); // 创建索引 drop index <code>索引名</code> on <code>表名</code>; // 删除索引 explain select * from <code>表名</code> where <code>字段名</code> = &#39;值&#39;; // 索引查询 show index from <code>表名</code>; // 显示索引信息</p>', 78)
  ]));
}
const mysql = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  mysql as default
};
